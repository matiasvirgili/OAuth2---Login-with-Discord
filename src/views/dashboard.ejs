<%- include('partials/header') -%>

<main class="bg-dark" style="padding-bottom: 150px">
  <div class="container text-light">
    <h1 style="padding-top: 10px">Dashboard</h1>

    <p><%- user.username %>, these are your server</p>

    <div class="row">
      <% guilds.forEach((guild) => { %> <%- include('partials/guild', { guild })
      -%> <% }) %>
    </div>
  </div>
</main>

<%- include('partials/footer') -%>
